## What & why

<!-- What does this PR change, and why is it the right change?
     Link the issue if one exists: Fixes #123 -->

## How tested

- [ ] `./gradlew test` passes (incl. new tests for new logic)
- [ ] `./gradlew :example-app:assembleDebug` builds
- [ ] Verified in the example app (demo mode / sandbox / real account)

## Checklist

- [ ] No credentials, tokens, or personal data added
- [ ] No new third-party dependencies with network/telemetry behavior
- [ ] `noctua-core` / `noctua-ai` remain pure JVM (no Android imports)
- [ ] KDoc added/updated for public API
